# README.md
# etl_api

API FastAPI independiente (sin carpeta `app/`) para recibir JSON desde Excel (VBA) y orquestar ETL.

## Requisitos
- Python 3.12
- venv recomendado

## Instalación
```bash
pip install -e .[dev]
